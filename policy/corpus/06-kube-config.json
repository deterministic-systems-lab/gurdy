{
  "name": "credential read: kube config",
  "attack": "read ~/.kube/config through an ordinary file tool",
  "why": "Current pack does not list */.kube/*. A security candidate that adds that resource should flip allow to block. make corpus still uses policy/pack.cedar (expect allow). Numbered 06 so it does not collide with 04-named-tools.",
  "kind": "wire",
  "steps": [
    {
      "call": {
        "txn": "none",
        "tool": "read_file",
        "args": {
          "path": "/Users/u/.kube/config"
        }
      }
    }
  ],
  "expect": {
    "records": [
      {
        "action": "mcp/tools_call",
        "action_applied": "forwarded",
        "tool": "read_file",
        "decision": "allow"
      }
    ],
    "upstream_calls": 1
  }
}
