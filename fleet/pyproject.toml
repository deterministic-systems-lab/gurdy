[project]
name = "gurdy-fleet"
version = "0.0.0"
description = "Gurdy fleet shipper: verify then push ledgers to the management console."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
gurdy-ship = "ship:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
